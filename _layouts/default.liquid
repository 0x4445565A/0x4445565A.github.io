<!DOCTYPE html>
<html>
  <head>
    <title>{{ page.title }}</title>
    {% include "head.liquid" %}
  </head>
  <!-- Look this HTML and CSS is NOT going to be pretty.  It is a means to an end.  I'm so sorry. -->
  <body>
    {% include "nav.liquid" %}
    {% include "editor.liquid" %}
    <div class="content">
      {{ page.content }}
    </div>
    {% include "footer.liquid" %}
    {% include "js.liquid" %}
  </body>
</html>